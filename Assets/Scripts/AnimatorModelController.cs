using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class AnimatorModelController
{
    public static class Params
    {
        public const string Move = nameof(Move);
        public const string Die = nameof(Die);
    }
}
